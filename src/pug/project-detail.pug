extends layout/default.pug

block variables
  - const bodyClass = ""
  - const pageTitle = "Детальная проекта"
  - const siteLang = "ru"
  - let page = "nne"

block content
  include pages/project-detail/main.pug
